# @qa/ui

Presentational components shared between surfaces of the web app.

Stage-1 placeholder — the run view, artifact viewer and test-case review UI are
built in T08/T11. shadcn/ui primitives are generated into `apps/web` by the
shadcn CLI and only promoted here once a second consumer needs them.
