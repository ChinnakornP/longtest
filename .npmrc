# Fail fast instead of silently hoisting: the executor sidecar must only see
# the dependencies it declares.
strict-peer-dependencies=false
auto-install-peers=true
# Never let a postinstall script run for a transitive dependency we did not
# explicitly allow. Add entries to onlyBuiltDependencies as they are reviewed.
enable-pre-post-scripts=false
