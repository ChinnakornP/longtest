{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://qa.local/schema/execution-result/1",
  "x-contract-version": "1.0.0",
  "title": "Execution result",
  "description": "What the Node executor reports back for one test case: per-step outcome, per-assertion outcome and the artifact keys that back them. This is the only evidence the Failure Analyst is allowed to reason from.",
  "$ref": "#/$defs/ExecutionResult",
  "$defs": {
    "ExecutionResult": {
      "type": "object",
      "additionalProperties": false,
      "required": ["version", "testCaseId", "result", "steps", "artifacts", "startedAt", "endedAt"],
      "properties": {
        "version": { "const": 1 },
        "testCaseId": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$" },
        "runId": { "type": "string", "format": "uuid" },
        "attempt": { "type": "integer", "minimum": 1, "maximum": 10 },
        "result": { "$ref": "#/$defs/Outcome" },
        "failureClass": { "$ref": "https://qa.local/schema/finding/1#/$defs/FailureClass" },
        "message": {
          "type": "string",
          "maxLength": 4000,
          "description": "Executor-authored summary. Never the raw page text."
        },
        "steps": {
          "type": "array",
          "maxItems": 100,
          "items": { "$ref": "#/$defs/StepResult" }
        },
        "assertions": {
          "type": "array",
          "maxItems": 50,
          "items": { "$ref": "#/$defs/AssertionResult" }
        },
        "artifacts": {
          "type": "array",
          "maxItems": 200,
          "items": { "$ref": "#/$defs/Artifact" }
        },
        "startedAt": { "type": "string", "format": "date-time" },
        "endedAt": { "type": "string", "format": "date-time" },
        "durationMs": { "type": "integer", "minimum": 0 }
      }
    },

    "Outcome": {
      "enum": ["pass", "fail", "skipped", "error"],
      "description": "fail is the application disagreeing with an assertion; error is the harness not getting far enough to have an opinion."
    },

    "StepResult": {
      "type": "object",
      "additionalProperties": false,
      "required": ["index", "action", "status"],
      "properties": {
        "index": {
          "type": "integer",
          "minimum": 0,
          "description": "Position in the test case steps array."
        },
        "action": {
          "$ref": "https://qa.local/schema/test-case/1#/$defs/StepAction"
        },
        "status": { "$ref": "#/$defs/Outcome" },
        "resolvedLocator": {
          "type": "string",
          "maxLength": 512,
          "description": "Which locator in the element fallback chain actually matched."
        },
        "unstableTarget": {
          "type": "boolean",
          "description": "True when the step used a raw locator instead of an application-map ref."
        },
        "message": { "type": "string", "maxLength": 4000 },
        "startedAt": { "type": "string", "format": "date-time" },
        "endedAt": { "type": "string", "format": "date-time" },
        "durationMs": { "type": "integer", "minimum": 0 },
        "artifactIds": {
          "type": "array",
          "maxItems": 20,
          "items": { "$ref": "#/$defs/ArtifactId" }
        }
      }
    },

    "AssertionResult": {
      "type": "object",
      "additionalProperties": false,
      "required": ["index", "type", "status"],
      "properties": {
        "index": { "type": "integer", "minimum": 0 },
        "type": {
          "$ref": "https://qa.local/schema/test-case/1#/$defs/AssertionType"
        },
        "status": { "$ref": "#/$defs/Outcome" },
        "expected": { "type": "string", "maxLength": 4000 },
        "actual": {
          "type": "string",
          "maxLength": 4000,
          "description": "Observed value, lifted off the page under test. Untrusted text."
        },
        "message": { "type": "string", "maxLength": 4000 },
        "artifactIds": {
          "type": "array",
          "maxItems": 20,
          "items": { "$ref": "#/$defs/ArtifactId" }
        }
      }
    },

    "ArtifactId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$",
      "description": "Run-local handle the daemon makes up so that steps, assertions and findings can point at the same evidence before anything has been stored. It is NOT the database id.",
      "$comment": "Two invariants JSON Schema cannot express, and that ingest therefore has to enforce (LONG-10): these ids must be unique within one run, and ingest must build a wireId -> artifacts.id (uuid) map from run.result.artifacts[] before writing finding_evidence. A duplicate id must be rejected, never last-write-wins."
    },

    "Artifact": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "kind", "key"],
      "properties": {
        "id": { "$ref": "#/$defs/ArtifactId" },
        "kind": {
          "enum": ["screenshot", "trace", "video", "network", "console", "dom", "report"]
        },
        "key": {
          "type": "string",
          "maxLength": 1024,
          "pattern": "^orgs/[A-Za-z0-9][A-Za-z0-9._-]{0,199}/runs/[0-9]{4}-[0-9]{2}-[0-9]{2}/[A-Za-z0-9][A-Za-z0-9._-]{0,199}/([A-Za-z0-9][A-Za-z0-9._-]{0,99}/)?[A-Za-z0-9][A-Za-z0-9._-]{0,199}$",
          "description": "Object storage key: orgs/{orgId}/runs/{YYYY-MM-DD}/{runId}/{testCaseId}/{filename}, with the {testCaseId} segment omitted for a run-level artifact such as a discovery HAR. That segment is a test case *ref* (TC-001), not the database uuid: the daemon builds the key from the test-case document it was handed and never sees the uuid the backend assigns. What carries the tenant boundary is the orgs/{orgId}/runs/{runId}/ prefix, which is the same prefix the presigned PUT policy is scoped to — so every segment is anchored to start with an alphanumeric, exactly as the artifacts_storage_key_layout CHECK does. A segment that could be \"..\" would make that prefix bound nothing, and the upload happens before the row is ever inserted: the database is the last gate here, not the first."
        },
        "contentType": { "type": "string", "maxLength": 128 },
        "sizeBytes": { "type": "integer", "minimum": 0 },
        "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
      }
    }
  }
}
